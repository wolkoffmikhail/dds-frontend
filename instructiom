Понял задачу: у тебя уже есть **существующий Next.js репозиторий** (на сервере и на GitHub), и ты не понимаешь **куда именно “влить” файлы из `bi-web-app.zip`**, чтобы не сломать текущее приложение.

По нашим соседним веткам проекта (где ты уже поднимал next.js + supabase, ловил ошибки env и “Failed to fetch”), самый безопасный путь такой:

## Лучший вариант для тебя

**Не заменять текущий проект целиком**, а:

1. создать в GitHub **новую ветку**
2. влить туда код v0 как “скелет страниц/компонентов”
3. смёржить аккуратно, только нужные файлы

Так ты не потеряешь текущую конфигурацию и работающий логин.

---

# Вариант 1 (рекомендую): влить архив в текущий репозиторий через новую ветку

## Шаг 1 — на сервере перейти в папку текущего проекта

Обычно это что-то типа:

```bash
cd /path/to/your-nextjs-project
```

Проверь, что это git-репо:

```bash
git status
```

## Шаг 2 — создать ветку

```bash
git checkout -b feature/bi-ui-from-v0
```

## Шаг 3 — распаковать архив ВРЕМЕННО в отдельную папку

Не в корень проекта сразу.

```bash
mkdir -p /tmp/bi-web-app
unzip /path/to/bi-web-app.zip -d /tmp/bi-web-app
ls /tmp/bi-web-app
```

## Шаг 4 — перенести в текущий проект ТОЛЬКО нужные папки

Обычно v0 даёт:

* `app/`
* `components/`
* `lib/`
* иногда `middleware.ts`
* иногда `styles/` или `tailwind.config.*`

Командами (пример):

```bash
cp -r /tmp/bi-web-app/app ./app
cp -r /tmp/bi-web-app/components ./components
cp -r /tmp/bi-web-app/lib ./lib
cp -r /tmp/bi-web-app/middleware.ts ./middleware.ts 2>/dev/null || true
```

⚠️ Важно: если у тебя уже есть `app/`, не “cp -r app app” вслепую. Лучше делать **слияние**:

* либо через `rsync` (аккуратнее),
* либо копировать конкретные под-папки (например `app/(protected)` и `app/login`).

Аккуратный вариант rsync:

```bash
rsync -av /tmp/bi-web-app/app/ ./app/
rsync -av /tmp/bi-web-app/components/ ./components/
rsync -av /tmp/bi-web-app/lib/ ./lib/
```

## Шаг 5 — сравнить package.json и поставить недостающие зависимости

Открой `/tmp/bi-web-app/package.json` и свой `package.json`.

Минимум надо, чтобы в твоём проекте были:

* `@supabase/supabase-js` (или `@supabase/ssr`, если v0 сделал SSR-куки)
* `recharts`
* `zod` (часто)
* `lucide-react` (часто)
* `tailwindcss` / `shadcn` (если используешь)

Быстро поставить ключевое:

```bash
npm i @supabase/supabase-js recharts
```

Если v0 использует `@supabase/ssr`:

```bash
npm i @supabase/ssr
```

## Шаг 6 — env НЕ затирать

`.env.local` не коммитим. Просто убедись что в нём:

```env
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
```

## Шаг 7 — запустить и проверить

```bash
npm install
npm run dev -- --hostname 0.0.0.0 --port 31001
```

Открыть:
`http://37.113.30.32:31001/auth/login`

## Шаг 8 — закоммитить в ветку и запушить

```bash
git add .
git commit -m "Add BI pages scaffold from v0"
git push -u origin feature/bi-ui-from-v0
```

Дальше на GitHub создаёшь Pull Request → аккуратно мёржишь.

---

# Вариант 2 (если боишься сломать существующий проект): отдельный фронтенд-проект

Ты поднимаешь `bi-web-app` как **второй Next.js сайт** рядом, на другом порту.
Плюсы: ноль риска.
Минус: два репозитория/деплоя.

---

# Что выбрать тебе (по нашему диалогу)

У тебя уже:

* Supabase self-hosted
* Next.js уже был, ошибки с env переменными раньше решали
* сейчас логин работает на `:31001`

Поэтому **лучше Вариант 1**: влить v0-код в текущий репо в отдельной ветке.

---

# Чтобы я дал точную инструкцию “куда именно копировать”

Мне нужно 2 факта (без вопросов не могу угадать структуру репо):

1. как называется папка проекта на сервере (путь)
2. используешь ли ты `app/` (App Router) или `pages/` (старый роутер)

Но чтобы не тормозить, вот быстрый способ узнать самому и сказать мне:

```bash
ls
```

Если есть папка `app/` — App Router.
Если есть папка `pages/` — Pages Router.

---

Если ты скинешь вывод:

```bash
ls -la
```

из корня твоего Next.js проекта — я скажу **какие именно папки из архива копировать**, чтобы 100% не сломать текущий сайт, и как правильно совместить middleware/auth.
